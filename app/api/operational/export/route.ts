import {authenticate} from '@/lib/production-auth';
import {row,errorResponse} from '@/lib/production-store';
import {movements,warehouse,material} from '@/lib/concise-model';
import {inBalanceScope} from '@/lib/concise-balance';
export async function GET(request:Request){try{const user=await authenticate(request),s=(await row()).payload,p=new URL(request.url).searchParams,from=p.get('from')||'0000-01-01',to=p.get('to')||s.today,scope=p.get('scope')||'all',selected=p.get('material')||'all';const quote=(v:unknown)=>'"'+String(v??'').replace(/^[=+@-]/,"'$&").replaceAll('"','""')+'"';const rows=[['Дата','Документ','Склад','Материал','Изменение, т','Основание'],...movements(s).filter(m=>m.date>=from&&m.date<=to&&inBalanceScope(s,scope,user.admin?'admin':user.employee,m.warehouse)&&(selected==='all'||m.material===selected)).map(m=>[m.date,m.docId,warehouse(s,m.warehouse),material(s,m.material),m.qty.toFixed(6).replace('.',','),m.basis])];return new Response('\ufeff'+rows.map(r=>r.map(quote).join(';')).join('\r\n'),{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':'attachment; filename="material-movements.csv"','Cache-Control':'no-store'}});}catch(e){return errorResponse(e);}}
