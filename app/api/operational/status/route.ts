import {runtime} from '@/lib/store';
export async function GET(){let operational=false,workflowVersion=0,notificationsEnabled=false;try{const e=runtime();if(e.pool&&e.OPERATIONAL_ROLLBACK!=='true'){const r=await e.pool.query("SELECT payload->>'dailyVersion' AS workflow_version FROM operational_state WHERE id=1");operational=r.rows.length>0;workflowVersion=Number(r.rows[0]?.workflow_version||0);notificationsEnabled=operational&&e.APP_MODE==='production'&&e.BOT_ENABLED==='true'&&!!e.TELEGRAM_BOT_TOKEN;}}catch{}return Response.json({operational,workflowVersion,notificationsEnabled},{headers:{'Cache-Control':'no-store'}});}
